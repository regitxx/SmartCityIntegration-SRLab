{
  "//": "Tailscale Serve config for the smcity sidecar. v0.4.14 points the sidecar at the nginx router (smcity-router:8080) instead of an agent replica directly. The router load-balances blue+green agent replicas and retries on the surviving instance during a rolling restart — that's what makes deploys zero-downtime.",

  "TCP": {
    "443": { "HTTPS": true }
  },

  "Web": {
    "${TS_CERT_DOMAIN}:443": {
      "Handlers": {
        "/": {
          "Proxy": "http://smcity-router:8080"
        }
      }
    }
  },

  "AllowFunnel": {
    "${TS_CERT_DOMAIN}:443": true
  }
}
